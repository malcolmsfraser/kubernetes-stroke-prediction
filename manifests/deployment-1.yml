apiVersion : apps/v1
kind: Deployment
metadata:
  name: "721final-d617"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "721final-d617"
  template:
    metadata:
      labels:
        app: "721final-d617"
    spec:
      containers:
        - name: "721final-d617"
          image: "721final.azurecr.io/721final"
          ports:
          - containerPort: 8080